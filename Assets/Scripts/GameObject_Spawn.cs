using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameObject_Spawn : MonoBehaviour
{
    //private GameObject_Spawn SpawnDefault()
    //{
    //    GameObject_Spawn myObj = GameObject_Spawn.Instantiate(this.templateObject, this.parent);
    //    myObj.SetActive(true);
    //    return myObj;
    //}

    //if(this.activeObject != null){
    //    GameObject_Spawn.Destroy(this.activeObject);
    //    this.activeObject = null;
    //}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
